# Compass

## Cursor Cloud specific instructions

This repository ("Compass") is currently empty (initialized with only a `README.md`).
There are no services, dependencies, build steps, lint checks, or tests at this time.

When code is added to the repository, update this section with:
- How to install dependencies
- How to run, build, lint, and test the application(s)
- Any non-obvious development environment caveats
